services:
  markus:
    container_name: markus
    build:
      context: '.'
    restart: unless-stopped
    volumes:
      - ./log/:/home/markus/log
      - ./config/:/home/markus/config
    logging:
      options:
        max-size: 50m
